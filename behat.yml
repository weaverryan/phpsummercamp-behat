default:
    extensions:
        Behat\MinkExtension:
            goutte: ~
            selenium2: ~
            base_url: http://localhost:8000/

    suites:
        default:
            contexts:
                - FeatureContext
                - AppContext
